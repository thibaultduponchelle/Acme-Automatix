on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Upload Acme::Automatix to CPAN
    steps:
    - name: Upload to CPAN
      id: upload
      uses: thibaultduponchelle/action-upload-to-cpan@master
    - name: Get the version that was uploaded
      run: echo "The version was ${{ steps.upload.outputs.version }}"

